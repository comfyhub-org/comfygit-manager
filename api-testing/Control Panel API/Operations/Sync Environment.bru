meta {
  name: Sync Environment
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/v2/comfygit/sync
  body: json
  auth: none
}

body:json {
  {
    "model_strategy": "skip",
    "remove_extra_nodes": true
  }
}

docs {
  Manually trigger environment sync to match git repository state.

  Request:
  - model_strategy: How to handle models ("skip"|"download"|"link", default: "skip")
  - remove_extra_nodes: Remove nodes not in pyproject.toml (default: true)

  Response:
  {
    "status": "success|error",
    "nodes_installed": ["package-1", "package-2"],
    "nodes_removed": ["package-3"],
    "errors": [],
    "message": "Sync completed|Sync completed with errors"
  }
}
