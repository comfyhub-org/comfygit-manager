meta {
  name: Update Config
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/v2/comfygit/config
  body: json
  auth: none
}

body:json {
  {
    "models_path": "/path/to/models",
    "civitai_api_key": "your-api-key"
  }
}

docs {
  Update workspace configuration settings.

  Request (all fields optional):
  - models_path: Path to shared models directory
  - civitai_api_key: CivitAI API token for model downloads
  - huggingface_token: Not yet supported
  - auto_sync_models: Not yet supported
  - confirm_destructive: Not yet supported

  Response:
  - 200 { "status": "updated" }
  - 400 { "error": "Invalid JSON|..." }
  - 500 { "error": "No environment detected" }
}
