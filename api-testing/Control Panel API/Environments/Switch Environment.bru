meta {
  name: Switch Environment
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/v2/comfygit/switch_environment
  body: json
  auth: none
}

body:json {
  {
    "target_env": "production"
  }
}

docs {
  Switch to a different environment. Triggers server restart (exit code 43).

  Request:
  - target_env: Target environment name (required)

  Response:
  - 400 { "error": "target_env required|Invalid JSON" }
  - 404 { "error": "Environment 'name' not found" }
  - 409 { "error": "Environment switch already in progress" }
  - 500 { "error": "Not in managed environment" }
  - 200 { "status": "switching", "message": "Switching to production..." }

  WARNING: Server restarts after 2s delay.
  Orchestrator daemon spawned on first switch.
}
