meta {
  name: Create Commit
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v2/comfygit/commit
  body: json
  auth: none
}

body:json {
  {
    "message": "Update workflows",
    "allow_issues": false
  }
}

docs {
  Commit workflow changes with safety checks.

  Request:
  - message: Commit message (default: "Update workflows")
  - allow_issues: Bypass workflow issue checks (default: false)

  Response cases:
  - 200 { "status": "no_changes" } - Nothing to commit
  - 400 { "status": "blocked", "reason": "workflows_with_issues", "issues": [...] } - Workflows have issues
  - 200 { "status": "success", "summary": { "new": N, "modified": N, "deleted": N } } - Success
}
