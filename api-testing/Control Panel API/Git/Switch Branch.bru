meta {
  name: Switch Branch
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/v2/comfygit/switch
  body: json
  auth: none
}

body:json {
  {
    "branch": "main",
    "force": false
  }
}

docs {
  Switch to a different branch. Triggers server restart (exit code 42).

  Request:
  - branch: Target branch name (required)
  - force: Skip uncommitted changes check (default: false)

  Response:
  - 400 { "error": "branch is required" }
  - 200 { "status": "warning", "reason": "uncommitted_changes", "message": "..." } - Needs force=true
  - 200 { "status": "success", "message": "Restarting to sync new branch..." }

  WARNING: Server restarts after 300ms delay.
}
