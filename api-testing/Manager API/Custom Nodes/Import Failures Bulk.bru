meta {
  name: Get Import Failures (Bulk)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/v2/customnode/import_fail_info_bulk
  body: json
  auth: none
}

body:json {
  {
    "node_ids": []
  }
}

docs {
  Returns bulk import failure information for specified nodes.
  Currently returns empty object as placeholder.
}
