meta {
  name: Get Queue Status
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v2/manager/queue/status
  body: none
  auth: none
}

docs {
  Get current task queue status.

  Response:
  {
    "history": { "task_id": {...} },
    "running_queue": [current_task] or [],
    "pending_queue": [queued_tasks],
    "installed_packs": { "node_id": {...} }
  }
}
